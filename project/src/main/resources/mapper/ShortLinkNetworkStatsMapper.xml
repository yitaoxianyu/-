<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nageoffer.shortlink.project.dao.mapper.ShortLinkNetworkStatsMapper">
    <insert id="insertStatsOrUpdate">
        INSERT INTO t_link_network_stats (
            full_short_url,
            gid,
            date,
            cnt,
            network,
            create_time,
            update_time,
            del_flag
        ) VALUES (
                     #{fullShortUrl},
                     #{gid},
                     #{date},
                     #{cnt},
                     #{network},
                     now(),
                     now(),
                     0
                 )
        ON DUPLICATE KEY UPDATE
                             cnt = cnt + #{cnt},
                             update_time = NOW();
    </insert>
    <select id="selectListByNetwork"
            resultType="com.nageoffer.shortlink.project.dao.entity.ShortLinkNetworkStatsDO">
        select count(*) as cnt,network from t_link_network_stats
        where gid = #{gid} and full_short_url = #{fullShortUrl}
          and date between #{start} and #{end} and del_flag = 0
        group by network;
    </select>
</mapper>